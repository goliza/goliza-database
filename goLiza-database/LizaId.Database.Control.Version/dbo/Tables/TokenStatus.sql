CREATE TABLE [dbo].[TokenStatus] (
    [idTokenStatus]   INT          NOT NULL,
    [NomeTokenStatus] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_TokenStatus] PRIMARY KEY CLUSTERED ([idTokenStatus] ASC)
);

