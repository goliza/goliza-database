CREATE TABLE [dbo].[UsuarioStatus] (
    [idUsuarioStatus]   INT          NOT NULL,
    [NomeUsuarioStatus] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_UsuarioStatus] PRIMARY KEY CLUSTERED ([idUsuarioStatus] ASC)
);

